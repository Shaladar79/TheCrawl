// scripts/config/register-settings.mjs
export function registerSystemSettings() {
  // Register settings later if needed.
  // Example:
  // game.settings.register("thecrawl", "debug", { name: "Debug", scope: "client", config: true, type: Boolean, default: false });
}

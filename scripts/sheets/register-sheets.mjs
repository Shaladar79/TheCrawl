// scripts/sheets/register-sheets.mjs
export function registerSheets() {
  // For now, we won’t register custom sheets until you create them.
  // This file exists so system.mjs stays stable as you grow.
  //
  // Example later:
  // Actors.unregisterSheet("core", ActorSheet);
  // Actors.registerSheet("thecrawl", TheCrawlActorSheet, { types: ["pc","npc","monster","companion","summon"], makeDefault: true });
  //
  // Items.unregisterSheet("core", ItemSheet);
  // Items.registerSheet("thecrawl", TheCrawlSkillSheet, { types: ["skill"], makeDefault: true });
  // Items.registerSheet("thecrawl", TheCrawlEquipmentSheet, { types: ["equipment"], makeDefault: true });

  console.log("The Crawl | Sheets registration placeholder");
}

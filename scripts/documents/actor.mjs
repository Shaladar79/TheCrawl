// scripts/documents/actor.mjs
export class TheCrawlActor extends Actor {
  prepareDerivedData() {
    super.prepareDerivedData();

    // Intentionally minimal for now.
    // Later you will compute derived stats (defense/initiative/speed) here.
  }
}
